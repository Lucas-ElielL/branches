# Exercício Branch

> Esta linha foi criada usando a branch `dev`

> Esta linha foi criada usando a branch `FEAT-1`; 

``` javascript
    console.log("Hello, World!");
```
> Esta linha foi criada para causar conflitos usando a Branch `dev`, mas não deveria. É apenas um teste.